/**
 * @file Конфигурация Next.js
 * @description Определяет основные настройки сборки, допустимые источники изображений
 *              и разрешённые домены для разработки. Этот файл обрабатывается Next.js
 *              при запуске сервера или сборке проекта.
 */

import type { NextConfig } from "next"

const nextConfig: NextConfig = {
  /**
   * Режим сборки "standalone".
   * Позволяет собрать автономный Docker-образ с минимальным набором файлов,
   * включая только необходимые node_modules и собранные артефакты.
   * Используется для деплоя в production-среде.
   */
  output: "standalone",

  /**
   * Разрешённые источники (origin) для режима разработки.
   * Указывает Next.js, с каких внешних доменов разрешено получать HMR-соединения.
   * Требуется при использовании ngrok/expose для доступа к dev-серверу извне.
   * @example "crate-diving-doorstop.ngrok-free.dev" — тоннель ngrok для тестирования на внешних устройствах.
   */
  allowedDevOrigins: ["crate-diving-doorstop.ngrok-free.dev"],

  /**
   * Настройки оптимизации изображений (next/image).
   * Определяет правила загрузки изображений с внешних CDN/хостингов.
   */
  images: {
    /**
     * Список разрешённых внешних хостов для изображений.
     * Изображения с других хостов будут отклонены на уровне next/image.
     */
    remotePatterns: [
      {
        /**
         * Протокол подключения к внешнему хранилищу.
         * Всегда HTTPS для безопасности.
         */
        protocol: "https",

        /**
         * Доменное имя CDN для файловых загрузок.
         * utfs.io — это UploadThing File Storage (бесплатный S3-совместимый хостинг).
         */
        hostname: "utfs.io",

        /**
         * Шаблон пути к файлам на CDN.
         * "/f/**" означает любые файлы внутри папки /f/.
         * Используется для хранения аватарок, вложений, изображений задач.
         */
        pathname: "/f/**",
      },
    ],
  },
}

export default nextConfig
